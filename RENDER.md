# Render.com deployment configuration

# Build Command: npm ci && npm run build
# Start Command: npm start
# Node Version: 18.x

# Environment Variables needed:
# NODE_ENV=production

# Static Files:
# This app requires COOP/COEP headers for WebGPU support
# Make sure to configure headers in render.yaml or web service settings